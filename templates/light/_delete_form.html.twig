<form method="post" action="{{ path('app_light_delete', {'id': light.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ light.id) }}">

    <hr class="mt-5 mb-3">

    <div class="alert alert-info mt-3">
        <i class="fas fa-info-circle"></i>
        <strong>Attention</strong> : cette action est irréversible.
        <p class="mt-2">
            À tout moment, vous pourrez créer une nouvelle lumière, cependant les données de celle-ci seront perdues.
        </p>
    </div>

    <div class="mx-auto d-flex justify-content-center">
        <button class="btn btn-danger mt-3 w-100">
            <i class="fas fa-heart-broken"></i>
            Effacer définitivement la lumière et dire au revoir à {{ light.firstname }}
        </button>
    </div>
</form>
